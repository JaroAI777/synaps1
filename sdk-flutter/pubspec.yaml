name: synapse_sdk
description: SYNAPSE Protocol SDK for Flutter applications - blockchain payments for AI agents
version: 1.0.0
homepage: https://synapse-protocol.ai
repository: https://github.com/synapse-protocol/flutter-sdk
documentation: https://docs.synapse-protocol.ai/sdk/flutter

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  web_socket_channel: ^2.4.0
  shared_preferences: ^2.2.2
  web3dart: ^2.7.1
  pointycastle: ^3.7.3
  convert: ^3.1.1
  bip39: ^1.0.6
  ed25519_hd_key: ^2.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  mockito: ^5.4.4
  build_runner: ^2.4.7

flutter:

platforms:
  android:
  ios:
  web:
  macos:
  windows:
  linux:
